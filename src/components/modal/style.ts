import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  dialogTitle: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
  },
  iconButton: {
      marginRight: 20,
  },
});
